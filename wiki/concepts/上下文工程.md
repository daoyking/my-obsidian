---
title: 上下文工程
aliases: [Context Engineering, 上下文管理]
type: concept
tags: [ai, llm, agent, context]
created: 2026-08-07
updated: 2026-09-01
sources: ["[[raw/papers/ai-agents-in-depth]]", "[[raw/articles/awesome-llm-apps]]"]
status: active
---

# 上下文工程

## 定义

系统设计与管理喂给 LLM 的上下文（系统提示、对话历史、工具结果、检索内容、状态信息），以最大化 Agent 能力上限。上下文是 Agent 的「眼睛」——决定模型此刻能看到什么、能推理什么。

## 为什么重要

- 《AI Agents in Depth》称上下文工程是全书最关键的一章：**Agent 的能力上限由上下文决定，不是模型本身**
- 同一模型，好上下文与差上下文效果天差地别
- 是 [[Harness工程]] 的核心组成

## 核心机制

来自 [[raw/papers/ai-agents-in-depth]] 第 2 章：

1. **API 上下文结构**：消息四种角色（system/user/assistant/tool），单轮与带工具的多轮
2. **KV Cache 友好设计**：上下文布局影响缓存命中率，进而影响延迟与成本；Prompt Cache 与 KV Cache 是两个层级
3. **提示工程**：人格（语气风格）、结构化提示、流程驱动 vs 规则堆砌、Few-shot、工具定义设计
4. **提示注入防御**：见 [[提示注入]]
5. **Agent Skills**：领域能力的可组合单元，按需加载（见本库 Skill 机制）
6. **Agent 状态栏**：用元信息增强轨迹管理（位置、时间感知）
7. **上下文压缩**：分层压缩；**子 Agent 上下文隔离优于压缩**

## 关键洞见

- 「隔离优于压缩」：把子任务交给独立子 Agent（隔离上下文）比在主上下文里压缩更有效——这正是本知识库 Ingest 时「一个来源交给 AI 单独编译」的理论依据
- 上下文学习的内部机制是**检索而非推理**——所以压缩要保留可检索的结构

## 与本知识库的关联

- 本库的 [[CLAUDE]]（Schema）就是上下文工程产物：告诉接入的 AI 如何看待这个 vault
- Ingest/Lint 流程是「把对的上下文在对的时机喂给模型」

## 成本控制：上下文优化工具（2026-09-01）

来自 [[raw/articles/awesome-llm-apps]] 的 `llm_optimization_tools`，两个工具把「上下文压缩」做成了可复用组件，也量化了这一节的收益：

| 工具 | 做法 | 声称收益 |
|---|---|---|
| Toonify Token Optimization | 用 TOON 格式（面向 token 的结构化数据表示）替代 JSON 等格式喂给模型 | 降低 **30–60%** API 成本 |
| Headroom Context Optimization | 上下文头部空间优化（裁剪/重组上下文布局） | 降低 **50–90%** API 成本 |

**与本节的关系**：上面第 2 点讲「KV Cache 友好设计」、第 7 点讲「上下文压缩」，这两个工具是它们的工程化落地——把「怎么组织上下文更省」从经验变成可插拔组件。

**注意**：这两个数字出自仓库自述，未见第三方基准验证。在多智能体场景（见 [[多智能体系统]]，协调模式 token 消耗是独立并行的 4.15 倍）下，成本优化的价值被放大，但也更需实测校准——[[基准测试]] 应先跑再信。

## 相关概念

- [[Harness工程]] · [[ReAct]] · [[提示注入]] · [[记忆系统]] · [[Prompt工程]] · [[基准测试]] · [[多智能体系统]]

## 相关实体

- [[awesome-llm-apps]]

## 相关主题

- [[AI-Agents-in-Depth]]

## 来源

- [[raw/papers/ai-agents-in-depth]]（第 2 章）
- [[raw/articles/awesome-llm-apps]]（LLM Optimization Tools 分类，2 个项目）
